/**
 * Mobile-specific enhancements - lazy loaded
 */

// Import mobile CSS when this module loads (lazy loaded)
import '../../css/mobile.css';

/**
 * Initialize mobile-specific features
 */
export function initMobileFeatures() {
  // Mobile-specific optimizations can be added here
  // For example: touch gestures, swipe actions, etc.
  // Mobile CSS is automatically loaded when this module is imported
}
