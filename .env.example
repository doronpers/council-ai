# Council AI - API Keys Configuration
# Copy this file to .env and add your actual API keys
# Never commit .env to version control (it's in .gitignore)

# Choose one or more providers (Council AI will automatically fallback if one fails):

# Anthropic Claude
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# OpenAI GPT
OPENAI_API_KEY=your-openai-api-key-here

# Vercel AI Gateway (OpenAI-compatible, can be used instead of OPENAI_API_KEY)
AI_GATEWAY_API_KEY=your-vercel-ai-gateway-key-here
# Optional: Custom Vercel AI Gateway URL (defaults to https://api.vercel.ai/v1)
# VERCEL_AI_GATEWAY_URL=https://your-gateway-url.vercel.app

# Google Gemini
GEMINI_API_KEY=your-gemini-api-key-here

# Text-to-Speech (TTS) API Keys
# ElevenLabs (Primary TTS provider - high quality, natural voices)
ELEVENLABS_API_KEY=your-elevenlabs-api-key-here

# OpenAI TTS (Fallback provider - uses same key as OPENAI_API_KEY above)
# No separate key needed if OPENAI_API_KEY is set

# Generic API Key (used if provider-specific key not found)
# Useful for custom HTTP endpoints
COUNCIL_API_KEY=your-generic-api-key-here

# Optional: Custom HTTP endpoint configuration
LLM_ENDPOINT=https://your-custom-endpoint.com/v1/completions
HTTP_API_KEY=your-http-endpoint-api-key-here

# Optional: Configuration directory override
# COUNCIL_CONFIG_DIR=/path/to/your/config
